export type LeadRoleta = {
  id?: number;
  nome: string;
  numero: string;
  created_at?: string;
};

export type Premios = {
  id?: number;
  nome_lead: string;
  numero_lead: string;
  premio: string;
  created_at?: string;
}; 